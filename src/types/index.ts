/**
 * Type Definitions Index
 * 
 * This module re-exports all type definitions from the main types.ts file.
 * New code should import types from this index or from specific sub-modules when available.
 * 
 * @example
 * // Good - import specific types
 * import type { Partner, Deal } from '@/types';
 * 
 * // Good - import from specific modules (when split)
 * import type { Partner } from '@/types/partner';
 * import type { Deal } from '@/types/deal';
 * 
 * @deprecated Import directly from '@/types' for now. 
 * This index will be split into sub-modules in a future refactor.
 */

// Re-export everything from the main types file
export * from '../types';
